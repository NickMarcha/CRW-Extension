# Required for admin login
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-secure-password

# Optional: host port for the server (default 3000)
# PORT=3002

# Optional: Cloudflare Tunnel. Required when using: docker compose --profile tunnel up
# CLOUDFLARE_TUNNEL_TOKEN=your-token-from-zero-trust-dashboard
